import Vue from 'vue'
import * as types from '../mutation-types'

const state = {
    homeTest: 222
}

const mutations = {
    
}

const getters = {
    
}

const actions = {
    
}

export default {
    namespaced: true,
    state,
    mutations,
    getters,
    actions
}
